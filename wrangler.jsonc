{
  // Static assets-only Worker: Astro builds to ./dist (fully static, no SSR adapter).
  // Assets config means no `main` entrypoint is needed.
  "name": "goatlab-scor-win",
  "compatibility_date": "2026-09-01",
  "assets": {
    "directory": "dist"
  }
}
